// Stub: original was a symlink to ../../../shared/pricing.js (not in this repo).
// Returns no adjustments so CartBar pricing falls through with the raw form.
export function adjustInvoice() {
  return {};
}
